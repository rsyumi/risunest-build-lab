{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://risunest.local/schemas/roadmap14-platform-result-v2.json",
  "title": "RisuNest Roadmap 14 platform result",
  "type": "object",
  "additionalProperties": false,
  "required": [
    "schemaVersion",
    "kind",
    "status",
    "scenario",
    "recordedAt",
    "fixture",
    "build",
    "platform",
    "memory",
    "ui",
    "latency",
    "bytes",
    "canonicalOutput",
    "source",
    "notes"
  ],
  "properties": {
    "schemaVersion": { "const": 2 },
    "kind": { "const": "risunest-roadmap14-platform-result" },
    "status": { "enum": ["completed", "pending", "failed"] },
    "scenario": {
      "enum": [
        "library-many",
        "save-large",
        "stream-postprocess",
        "asset-library"
      ]
    },
    "recordedAt": { "type": "string", "format": "date-time" },
    "fixture": { "$ref": "#/$defs/fixture" },
    "build": { "$ref": "#/$defs/build" },
    "platform": { "$ref": "#/$defs/platform" },
    "memory": { "$ref": "#/$defs/memory" },
    "ui": { "$ref": "#/$defs/ui" },
    "latency": { "$ref": "#/$defs/latency" },
    "bytes": { "$ref": "#/$defs/bytes" },
    "canonicalOutput": {
      "anyOf": [
        { "$ref": "#/$defs/canonicalOutput" },
        { "type": "null" }
      ]
    },
    "source": { "$ref": "#/$defs/source" },
    "notes": {
      "type": "array",
      "items": { "type": "string" }
    }
  },
  "allOf": [
    {
      "if": {
        "properties": { "status": { "const": "completed" } },
        "required": ["status"]
      },
      "then": {
        "properties": {
          "build": {
            "properties": {
              "sourceRevision": { "$ref": "#/$defs/gitRevision" }
            }
          },
          "canonicalOutput": { "$ref": "#/$defs/canonicalOutput" },
          "memory": {
            "properties": {
              "measurement": { "enum": ["js-heap-and-rss", "android-pss"] }
            },
            "anyOf": [
              { "properties": { "heapUsedBytes": { "minItems": 1 } } },
              { "properties": { "rssBytes": { "minItems": 1 } } },
              { "properties": { "pssBytes": { "minItems": 1 } } }
            ]
          },
          "ui": {
            "properties": {
              "domNodeCount": { "type": "integer", "minimum": 0 },
              "mountedMessageCount": { "type": "integer", "minimum": 0 },
              "liveUrlCount": { "type": "integer", "minimum": 0 }
            }
          },
          "latency": {
            "properties": { "samples": { "minItems": 1 } }
          },
          "bytes": {
            "properties": { "artifacts": { "minItems": 1 } }
          },
          "source": {
            "properties": {
              "measurements": { "minItems": 1 },
              "artifacts": { "minItems": 1 }
            }
          }
        }
      }
    }
  ],
  "$defs": {
    "sha256": {
      "type": "string",
      "pattern": "^[0-9a-f]{64}$"
    },
    "gitRevision": {
      "type": "string",
      "pattern": "^[0-9a-f]{40}$"
    },
    "nullableString": {
      "type": ["string", "null"]
    },
    "nullableCount": {
      "type": ["integer", "null"],
      "minimum": 0
    },
    "samples": {
      "type": "array",
      "items": {
        "type": "number",
        "minimum": 0
      }
    },
    "fixture": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "version", "identitySha256", "descriptor"],
      "properties": {
        "name": {
          "enum": [
            "library-many",
            "save-large",
            "stream-postprocess",
            "asset-library"
          ]
        },
        "version": { "type": "integer", "minimum": 1 },
        "identitySha256": { "$ref": "#/$defs/sha256" },
        "descriptor": { "type": "object" }
      }
    },
    "build": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "identity",
        "sourceRevision",
        "profile",
        "target",
        "appVersion",
        "realmDisabled"
      ],
      "properties": {
        "identity": { "type": "string", "minLength": 1 },
        "sourceRevision": { "$ref": "#/$defs/nullableString" },
        "profile": { "enum": ["release", "profile", "debug"] },
        "target": { "type": "string", "minLength": 1 },
        "appVersion": { "type": "string", "minLength": 1 },
        "realmDisabled": { "const": true }
      }
    },
    "platform": {
      "type": "object",
      "additionalProperties": false,
      "required": [
        "family",
        "identity",
        "osVersion",
        "architecture",
        "webViewVersion",
        "deviceModel"
      ],
      "properties": {
        "family": { "enum": ["windows", "android"] },
        "identity": { "type": "string", "minLength": 1 },
        "osVersion": { "type": "string", "minLength": 1 },
        "architecture": { "type": "string", "minLength": 1 },
        "webViewVersion": { "$ref": "#/$defs/nullableString" },
        "deviceModel": { "$ref": "#/$defs/nullableString" }
      }
    },
    "memory": {
      "type": "object",
      "additionalProperties": false,
      "required": ["measurement", "heapUsedBytes", "rssBytes", "pssBytes"],
      "properties": {
        "measurement": {
          "enum": ["js-heap-and-rss", "android-pss", "pending"]
        },
        "heapUsedBytes": { "$ref": "#/$defs/samples" },
        "rssBytes": { "$ref": "#/$defs/samples" },
        "pssBytes": { "$ref": "#/$defs/samples" }
      }
    },
    "ui": {
      "type": "object",
      "additionalProperties": false,
      "required": ["domNodeCount", "mountedMessageCount", "liveUrlCount"],
      "properties": {
        "domNodeCount": { "$ref": "#/$defs/nullableCount" },
        "mountedMessageCount": { "$ref": "#/$defs/nullableCount" },
        "liveUrlCount": { "$ref": "#/$defs/nullableCount" }
      }
    },
    "latencySample": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "valuesMs"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "valuesMs": {
          "allOf": [
            { "$ref": "#/$defs/samples" },
            { "minItems": 1 }
          ]
        }
      }
    },
    "latency": {
      "type": "object",
      "additionalProperties": false,
      "required": ["samples"],
      "properties": {
        "samples": {
          "type": "array",
          "items": { "$ref": "#/$defs/latencySample" }
        }
      }
    },
    "byteArtifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "bytes"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "bytes": { "type": "integer", "minimum": 0 }
      }
    },
    "bytes": {
      "type": "object",
      "additionalProperties": false,
      "required": ["artifacts"],
      "properties": {
        "artifacts": {
          "type": "array",
          "items": { "$ref": "#/$defs/byteArtifact" }
        }
      }
    },
    "canonicalOutput": {
      "type": "object",
      "additionalProperties": false,
      "required": ["sha256", "provenance"],
      "properties": {
        "sha256": { "$ref": "#/$defs/sha256" },
        "provenance": { "type": "string", "minLength": 1 }
      }
    },
    "sourceArtifact": {
      "type": "object",
      "additionalProperties": false,
      "required": ["name", "sha256"],
      "properties": {
        "name": { "type": "string", "minLength": 1 },
        "sha256": { "$ref": "#/$defs/sha256" }
      }
    },
    "source": {
      "type": "object",
      "additionalProperties": false,
      "required": ["runner", "measurements", "artifacts"],
      "properties": {
        "runner": { "type": "string", "minLength": 1 },
        "measurements": {
          "type": "array",
          "items": { "type": "string", "minLength": 1 }
        },
        "artifacts": {
          "type": "array",
          "items": { "$ref": "#/$defs/sourceArtifact" }
        }
      }
    }
  }
}
